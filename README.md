# Hornets Hive Status

Public status page for Hornets IT Solutions' background agents — **status and
timing only**. No revenue figures, prospect counts, or run detail; that stays
on the private Tailscale-only dashboard.

- `index.html` — the page itself
- `status.json` — same data as JSON, for programmatic reads

Regenerated automatically after every agent run.
